<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Server action: Track Stock button on PO form toolbar -->
        <record id="action_track_consignment_stock" model="ir.actions.server">
            <field name="name">Track Consignment Stock</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_view_types">form</field>
            <field name="state">code</field>
            <field name="code">
if record.is_consignment:
    action = record.action_track_consignment_stock()
            </field>
        </record>

    </data>
</odoo>
