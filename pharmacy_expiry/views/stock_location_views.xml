<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- SC2-UC-01: Add "Expired Location" checkbox to location form -->
    <record id="stock_location_form_expiry" model="ir.ui.view">
        <field name="name">stock.location.form.expiry</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='usage']" position="after">
                <field name="is_expired_location"/>
            </xpath>
        </field>
    </record>

</odoo>
